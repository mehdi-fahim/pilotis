{% extends 'base.html.twig' %}

{% block title %}Nouvel acteur - Pilotis{% endblock %}
{% block page_title %}Nouvel acteur{% endblock %}

{% block body %}
<div class="card-pilotis form-card"><div class="card-body">
    {{ form_start(form) }}
        <div class="row g-3">
            <div class="col-md-6">{{ form_row(form.firstName) }}</div>
            <div class="col-md-6">{{ form_row(form.lastName) }}</div>
            <div class="col-md-6">{{ form_row(form.role) }}</div>
            <div class="col-md-6">{{ form_row(form.department) }}</div>
            <div class="col-md-6">{{ form_row(form.email) }}</div>
            <div class="col-md-6">{{ form_row(form.phone) }}</div>
            <div class="col-12">{{ form_row(form.notes) }}</div>
        </div>
        <button class="btn btn-primary btn-pilotis">Créer</button>
        <a href="{{ path('app_actor_index') }}" class="btn btn-outline-secondary btn-pilotis">Annuler</a>
    {{ form_end(form) }}
</div></div>
{% endblock %}
